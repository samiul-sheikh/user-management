import React from 'react';
import UserData from '../UserData/UserData';

const Homepage = () => {
    return (
        <div>
            <UserData />
        </div>
    );
};

export default Homepage;